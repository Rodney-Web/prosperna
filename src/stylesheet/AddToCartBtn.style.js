import { StyleSheet } from "react-native";

export default StyleSheet.create({
    addViewCart: {
      backgroundColor: "#fff"
    },
    btnText: {
      color: "#000",
      fontWeight: "700",
    }
  })