import { StyleSheet } from "react-native";

export default StyleSheet.create({
    editFormBtn: {
        backgroundColor: "#000",
        padding: 10,
        borderRadius: 30,
        position: "absolute",
        right: 5,
        bottom: 0
      },
      editFormBtnText: {
        color: "#fff",
        textAlign: "center"
      }
})